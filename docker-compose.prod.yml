services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: grakchawwaa-bot
    env_file:
      - .env
    environment:
      NODE_ENV: production
    restart: unless-stopped
